export interface InstagramUser {
    data: {
        id: string;
        media_type?: string;
        media_url?: string;
        username: string;
        timestamp?: string;
        media_count?: string;
        account_type?: string;
    };
}
